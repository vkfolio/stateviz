# StateViz

StateViz is a VS Code extension that detects opted-in Python LangGraph `StateGraph`
files and renders Mermaid diagrams in a side panel.

## Marker

Add this comment to a Python file to opt in:

```python
# stateviz: langgraph
```

## Development

```bash
npm install
npm run build
```
